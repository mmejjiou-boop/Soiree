<form method="post" action="{{ path('app_dj_delete', {'id': dj.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dj.id) }}">
    <button class="btn">Delete</button>
</form>
