{% extends 'base.html.twig' %}

{% block title %}Dj{% endblock %}

{% block body %}
    <h1>Dj</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dj.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ dj.nom }}</td>
            </tr>
            <tr>
                <th>Style</th>
                <td>{{ dj.style }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dj_index') }}">back to list</a>

    <a href="{{ path('app_dj_edit', {'id': dj.id}) }}">edit</a>

    {{ include('dj/_delete_form.html.twig') }}
{% endblock %}
